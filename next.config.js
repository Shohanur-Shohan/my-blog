/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['https://images.unsplash.com', 'roar.media', 'media.istockphoto.com']
    },
    // distDir: 'build',   
}

module.exports = nextConfig
